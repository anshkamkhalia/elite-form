services:
  typesense:
    image: typesense/typesense:27.1
    restart: unless-stopped
    ports:
      - "8108:8108"
    volumes:
      - typesense-data:/data
    command: >-
      --data-dir /data
      --api-key=${TYPESENSE_API_KEY:-atlas-dev-key}
      --enable-cors

volumes:
  typesense-data:
